/// <reference types="node" />
import { Application } from "express";
import { Server } from "http";
import { OutputMockFile } from "./compilerOutRootFiles";
import MockServerOptions from "./mockServerOptions";
export default function mockServer(attachedFileNames: OutputMockFile[], options: MockServerOptions, listenCallback: (port: number, server: Server) => void): Promise<Application>;
